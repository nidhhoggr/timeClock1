{% extends '::base.html.twig' %}

{% block content -%}
    <h1>EntryType</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Entryid</th>
                <td>{{ entity.entryId }}</td>
            </tr>
            <tr>
                <th>Isclockin</th>
                <td>{{ entity.isClockin }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('entrytype') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('entrytype_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
